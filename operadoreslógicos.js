/* 
    == ---- igual ----- a==b ---- verdadeiro se a for igual a b
    === ---- idêntico ---- a===b ---- verdadeiro se a for idêntico a b
    !== ---- não idêntico ---- a!==b ----- falso se a for idêntico a b
    != ---- diferente ---- a != b ---- verdadeiro se o a não for idêntico ao b
    < ---- menor que ---- a < b ---- verdadeiro quando a for menor do que b
    > ---- maior que ---- a > b ---- verdadeiro quando a for maior do que b
    <= ---- menor igual que ---- a <= b ---- verdadeiro quando for menor ou igual a b
    >= maior igual que ---- a >= b ---- verdadeiro quando a for maior ou igual a b
*/

const a = 2

const b = 3

console.log(a < b)